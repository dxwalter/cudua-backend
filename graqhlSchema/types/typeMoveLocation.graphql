
type dataRes {
    code: Int,
    success: Boolean
    message: String
}

# Do not worry about this feature. I used it to move the street data from mysql to mongodb
type Mutation {
    moveData:dataRes 
}